import React from 'react'

const DrugMoreInfo = ({ drug }) => {

    return(
        <div>
            <a href={null}>L</a>
            <button>Delete</button>
        </div>
    )
}

export default DrugMoreInfo 